applications:
- name:      cf-env-test
  mem:       128M

  # note that you will probably need to change the `host` value below,
  # since host names are global across CloudFoundry

  host:      cf-env-test
